node_modules/
test-results/
test-results.json
playwright-report/

# Local-only: synced from deploy/transcripts (not tracked — 246 MB, deploy is canonical)
transcripts/

# Shard build intermediate (655 MB — exceeds GitHub's 100 MB file limit; runtime uses data/shards/)
data/ppp_sentences_full_en.db

# 2026-08-25: no consumer (db.js loadSentencesDB() removed 2026-07-27; live
# sentence-search path is data/shards/*.db.br). Same bomb as deploy/ — do not
# re-add. See scripts/build_offline_packs.py and scripts/promote_to_deploy.py.
data/ppp_sentences_en.db
data/ppp_sentences_en.db.gz

# 2026-08-25: dead April 2nd DBs, zero consumers found (staging/js, index.html,
# tests, manifest.json, scripts/, backup/daily_update.py, .github/workflows/ all
# checked). deploy/.assetsignore explicitly excludes them from publishing.
# ppp_transcripts_html_* superseded by individual HTML files (project_html_transcript_db_legacy.md).
data/ppp_concepts_en.db
data/ppp_transcripts_html_en.db
data/ppp_transcripts_html_lv.db
data/ppp_transcripts_html_ru.db

# 2026-08-25: local benchmark artifacts (android profiling, memory sampler
# output, throttle test config/results) — not part of the app, not deployed,
# never committed. Kept on disk for local perf work only.
bench/
